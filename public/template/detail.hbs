<div class="" ng-controller="detailController as ctrl">
    <h1>detail</h1>
    <p>{{ctrl.message}}</p>
    <button ng-click="ctrl.currentTab='tab1'">
        照片
    </button>
    <button ng-click="ctrl.currentTab='tab2'">
        简历
    </button>
    <button ng-click="ctrl.currentTab='tab3'">
        经历
    </button>
    <button ng-click="ctrl.currentTab='default'">
        个人主页
    </button>
    <div ng-switch="ctrl.currentTab">
        <div ng-switch-when="tab1">
            照片：
            <div ng-init="mysrc = 'images/min.jpg'"></div>
            <img src="{{mysrc}}" alt="" width="200">
        </div>
        <div ng-switch-when="tab2">
            简历：
            <p ng-pluralize
               count="ctrl.personCount"
               when="{'0':'Nobody is viewing.',
               'one','1 person is viewing',
               'other','{} people are viewing.'"></p>
        </div>
        <div ng-switch-when="tab3">
            经历：
            <input type="text"
                   ng-blur="ctrl.focusState = 'Blurred'"
                   ng-focus="ctrl.focusState='Focused'"
                   ng-mouseenter="ctrl.mouseState='Entered'"
                   ng-mouseleave="ctrl.mouseState='left'"
                   ng-mouseclick="ctrl.mouseState='Clicked'"
                   ng-mousedown="ctrl.mouseState='Down'"
                   ng-mouseup="ctrl.mouseState='Up'"
                   ng-keyup="ctrl.keyStroke($event)"
                   ng-click="ctrl.mouseClick($event)"
            ><hr>
            Focus State:{{ctrl.focusState}}<br>
            Mouse State:{{ctrl.mouseState}}<br>
            Key Info:{{ctrl.keyInfo | json}}<br>
            Mouse Info:{{ctrl.mouseInfo | json}}<br>
        </div>
        
        <div ng-switch-when="tab4"></div>
        <div ng-switch-default>没有</div>
    </div>
</div>